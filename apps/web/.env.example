# GraphQL endpoint served by Hasura (Envio indexer)
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:8080/v1/graphql

# HyperSync endpoint for EVM comparison data (server-side only)
HYPERSYNC_ENDPOINT=https://arbitrum.hypersync.xyz
